# Final Submission Checklist

- [x] Modular Cypress test files
- [x] `cy.login()` custom command
- [x] `cy.addProduct()` custom command
- [x] Additional `cy.loginWithSession()` custom command
- [x] Fixture-based test (`login.fixture.cy.js`)
- [x] Environment variable test (`login.env.cy.js`)
- [x] `cy.session()` test (`session.cy.js`)
- [x] `cy.intercept()` test (`intercept.cy.js`)
- [x] Flaky scenario documented and fixed (`flaky-fixed.cy.js`)
- [ ] Run the tests locally
- [ ] Generate `cypress/reports/mochawesome-report.html`
- [ ] Review actual pass/fail counts
- [x] 150-250 word reflection (`REFLECTION.md`)
- [ ] Upload the project to GitHub OR submit the final ZIP
